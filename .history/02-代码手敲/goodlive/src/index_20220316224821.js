import React from 'react';
import ReactDOM from 'react-dom';
import './style.less'

ReactDOM.render(
  <React.StrictMode>
    h2 {'Hello, world!'}
  </React.StrictMode>,
  document.getElementById('root')
);

